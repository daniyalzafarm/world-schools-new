# =============================================================================
# PROD_SUPERADMIN_ENV — example reference
# =============================================================================
#
# Paste into GitHub Variable `PROD_SUPERADMIN_ENV`. Injected as runtime env on
# `ca-admin-wc-prod`. None of these values are secret — see Phase 2 of the
# deployment plan.
# =============================================================================

APP_URL=https://superadmin.world-camps.org
API_BASE_URL=https://api.world-camps.org
WS_URL=https://api.world-camps.org
PROVIDER_APP_URL=https://provider.world-camps.org
BOOKING_APP_URL=https://booking.world-camps.org
AUTH_USING_REQUEST=false
