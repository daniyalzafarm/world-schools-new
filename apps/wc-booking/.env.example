# Runtime config (read at request time on the server, injected to
# window.__APP_CONFIG__ via the root layout's inline <script>).
# No NEXT_PUBLIC_* prefix — env vars are NOT baked into the build.

# App URL (used as metadataBase for SEO / OpenGraph)
APP_URL=http://localhost:4303

# API Configuration
# Base URL for the wc-nest-api backend (user module)
API_BASE_URL=http://localhost:3000/

# WebSocket Configuration
# WebSocket server URL for real-time messaging (gateway runs on the API's HTTP port)
WS_URL=http://localhost:3000

# Authentication Configuration
# Set to 'true' to use request-based auth (tokens in headers)
# Set to 'false' to use cookie-based auth (HTTP-only cookies) - recommended for production
AUTH_USING_REQUEST=false

# App Version (automatically set during CI/CD deployment)
APP_VERSION=dev

# Google Maps JS API (enable Maps JavaScript API + Places API (New) for map + business place cards)
GOOGLE_MAPS_API_KEY=

# Google OAuth client ID for "Sign in with Google" (public, browser-exposed; no
# secret). Same value as the API's GOOGLE_CLIENT_ID. Leave blank to disable Google
# auth (the UI then hides the Google button + One Tap). e.g. xxxxx.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_ID=

# Stripe publishable key (`pk_test_…` locally, `pk_live_…` in prod)
STRIPE_PUBLISHABLE_KEY=

# WebSocket Feature Flags
# Enable WebSocket-based message sending (default: true, set to false to use HTTP)
# Note: Real-time receiving (incoming messages, typing, presence) always works via WebSocket
ENABLE_WEBSOCKET_MESSAGES=true
# Enable HTTP fallback when WebSocket is unavailable (default: true)
WEBSOCKET_FALLBACK_HTTP=true
