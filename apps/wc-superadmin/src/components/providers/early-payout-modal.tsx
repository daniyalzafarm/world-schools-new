// Phase 8: deprecated. Replaced by `payout-mode-modal.tsx`. Kept as a thin
// re-export for any straggler imports — remove on the next sweep once all
// callers reference the new modal directly.
export { PayoutModeModal as EarlyPayoutModal } from './payout-mode-modal'
